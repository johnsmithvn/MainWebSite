# =============================================================================
# 🏠 MainWebSite React App Environment Configuration Template
# =============================================================================
# Copy file này thành .env và điền các giá trị thực tế của bạn
# Cấu hình môi trường cho ứng dụng React quản lý media (manga, movie, music)
# Các biến VITE_* được expose cho client-side, NODE_ENV cho build process
# =============================================================================

# =============================================================================
# 🌐 DEVELOPMENT SERVER & NETWORK CONFIGURATION
# =============================================================================
# Cấu hình cho Vite dev server và Hot Module Replacement (HMR)

# VITE_HMR_HOST: Hostname cho HMR connection
# Thay thế bằng hostname/domain thực tế của bạn
VITE_HMR_HOST=your-hostname.local

# VITE_HMR_PORT: Port cho HMR websocket connection
# Port khác với main app để tránh conflict
VITE_HMR_PORT=3001

# VITE_ALLOWED_HOSTS: Whitelist hosts được phép access dev server
# Thay thế bằng hostname và IP thực tế của bạn
VITE_ALLOWED_HOSTS=your-hostname.local,192.168.1.xxx

# =============================================================================
# 🔌 API & BACKEND COMMUNICATION
# =============================================================================
# Cấu hình endpoint để React app giao tiếp với Express backend

# VITE_API_BASE_URL: Base URL cho tất cả API calls
# Development: localhost, Production: domain/IP thực tế của bạn
VITE_API_BASE_URL=http://localhost:3000
# Production: uncomment và điền domain/IP thực tế
# VITE_API_BASE_URL=https://your-domain.com:3000

# =============================================================================
# 🔧 DEVELOPMENT & BUILD CONFIGURATION
# =============================================================================
# Cài đặt cho quá trình development và build

# VITE_BASE: Base path cho routing (thường là "/" cho SPA)
# Dùng khi deploy app ở subfolder
VITE_BASE=/

# VITE_DISABLE_HMR: Tắt Hot Module Replacement nếu gặp vấn đề
# Uncomment khi HMR gây lag hoặc error
# VITE_DISABLE_HMR=true

# =============================================================================
# 🚀 PRODUCTION & ENVIRONMENT SETTINGS
# =============================================================================
# Cài đặt cho production deployment (uncomment khi deploy)

# NODE_ENV: Environment mode cho build process
# production = optimize, minify code
# NODE_ENV=production

# VITE_ENABLE_HTTPS: Bật HTTPS cho production
# Dùng với reverse proxy có SSL certificate
# VITE_ENABLE_HTTPS=true

# VITE_MIN_STORAGE_SPACE: Minimum free space required (bytes)
# 524288000 = 500MB - để cache manga/video thumbnails
VITE_MIN_STORAGE_SPACE=524288000

# =============================================================================
# 📱 PWA (Progressive Web App) CONFIGURATION
# =============================================================================
# Cấu hình cho PWA manifest và service worker

# VITE_APP_NAME: Tên đầy đủ của app hiển thị khi install PWA
# Thay đổi thành tên app của bạn
VITE_APP_NAME=Your App Name

# VITE_APP_SHORT_NAME: Tên ngắn hiển thị trên home screen
# Tên ngắn cho icon trên home screen
VITE_APP_SHORT_NAME=YourApp

# VITE_APP_DESCRIPTION: Mô tả app trong manifest.json
# Mô tả app của bạn
VITE_APP_DESCRIPTION=Your media management application

# =============================================================================
# 🎨 UI/UX & THEME CONFIGURATION
# =============================================================================
# Cài đặt giao diện và theme

# VITE_DEFAULT_THEME: Theme mặc định khi load app lần đầu
# Options: "light", "dark", "auto"
VITE_DEFAULT_THEME=light

# VITE_ENABLE_DARK_MODE: Cho phép chuyển đổi dark/light mode
# false = chỉ dùng light theme
VITE_ENABLE_DARK_MODE=true

# =============================================================================
# 📊 PERFORMANCE & CACHING SETTINGS  
# =============================================================================
# Cài đặt hiệu suất và cache cho tốc độ load

# VITE_PRELOAD_COUNT: Số lượng items preload trước
# Manga reader: preload next N pages
VITE_PRELOAD_COUNT=10

# VITE_CACHE_SIZE: Số lượng items cache trong memory
# Balance giữa performance và RAM usage
VITE_CACHE_SIZE=100

# VITE_PAGINATION_SIZE: Số items hiển thị mỗi page
# Grid view: manga/movie cards per page
VITE_PAGINATION_SIZE=20

# =============================================================================
# 🔒 SECURITY & AUTH CONFIGURATION
# =============================================================================
# Cài đặt bảo mật và xác thực

# VITE_ENABLE_AUTH: Bật/tắt authentication system
# false = public access, true = require login
VITE_ENABLE_AUTH=true

# VITE_SESSION_TIMEOUT: Thời gian timeout session (ms)
# 3600000 = 1 hour, sau đó auto logout
VITE_SESSION_TIMEOUT=3600000

# =============================================================================
# 🐛 DEBUG & DEVELOPMENT TOOLS
# =============================================================================
# Công cụ debug chỉ dùng trong development

# VITE_DEBUG_MODE: Hiển thị debug info trong console
# true = show API calls, state changes, errors
VITE_DEBUG_MODE=false

# VITE_SHOW_PERFORMANCE_METRICS: Hiển thị performance metrics
# true = show load times, render performance
VITE_SHOW_PERFORMANCE_METRICS=false

# =============================================================================
# 📝 SETUP INSTRUCTIONS
# =============================================================================
# 1. Copy file này thành .env: cp .env.template .env
# 2. Thay thế các placeholder bằng giá trị thực tế:
#    - your-hostname.local → hostname thực tế
#    - 192.168.1.xxx → IP address thực tế
#    - your-domain.com → domain production thực tế
#    - Your App Name → tên app của bạn
# 3. Đối với production, uncomment các biến cần thiết
# 4. Không commit file .env vào git (đã có trong .gitignore)