# =============================================================================
# 🏠 MainWebSite React App Environment Configuration 
# =============================================================================
# Chỉ chứa các biến thực sự được sử dụng trong ứng dụng
# Đã loại bỏ các biến thừa không được sử dụng
# =============================================================================

# =============================================================================
# 🚀 CORE SETTINGS (Actually Used)
# =============================================================================

# Basic Configuration
VITE_BASE=/

# kiểm tra bộ nhớ còn bao nhiêu trước khi download
VITE_MIN_STORAGE_SPACE=10 # in MB

# =============================================================================
# 🛠️ DEVELOPMENT ONLY SETTINGS
# =============================================================================
# ⚠️ Comment out khi deploy production

# Development Environment
NODE_ENV=development  
# NODE_ENV=development  --- IGNORE ---

# Development Server & HMR
# TAILSCALE_TAILNET và TAILSCALE_DEVICE từ backend/.env
VITE_HMR_HOST=TAILSCALE_DEVICE.TAILSCALE_TAILNET.ts.net
VITE_HMR_PORT=3001
# host alloww thêm ngoài các host đã fix cứng trong vite.config.js
# ALLOWED_IPS từ backend/.env
VITE_ALLOWED_HOSTS=ALLOWED_IPS 

# Development Debug Tools (Optional)
# VITE_DISABLE_STRICT_MODE=true
# VITE_DISABLE_HMR=true

# =============================================================================
# 🚀 PRODUCTION ONLY SETTINGS
# =============================================================================
# ⚠️ Uncomment khi deploy production, comment Development section

# khi deploy production thì uncomment dòng dưới, comment dòng dev ở trên trong DEVELOPMENT ONLY SETTINGS
# NODE_ENV=production
# VITE_ENABLE_HTTPS=true

# =============================================================================
# 📋 PRODUCTION DEPLOYMENT NOTES
# =============================================================================
# Khi deploy production:
# 1. ✅ Comment out "DEVELOPMENT ONLY SETTINGS" section
# 2. ✅ Uncomment "PRODUCTION ONLY SETTINGS" section  
# 3. ✅ Set NODE_ENV=production
# 4. ✅ Set VITE_ENABLE_HTTPS=true if using HTTPS
# API calls sử dụng Vite proxy (dev) hoặc relative URLs (prod)