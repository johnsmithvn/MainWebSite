# 📁 backend/.env.template  
# 🔧 Environment Configuration Template
# 📝 Copy this file to .env và điền các giá trị thực tế

# 🌐 SERVER CONFIGURATION
PORT=3000
NODE_ENV=development    # không cần sửa do trong package.json đã set env thành production/development
ENABLE_HTTPS=true       # Bật HTTPS cho server (nếu có SSL cert)


# 📂 MEDIA ROOT PATHS
# Đường dẫn tới các thư mục media trên disk

# 📚 Manga paths (ROOT_ prefix)
ROOT_DOW=E:/File/Manga/DOW
ROOT_FANTASY=E:/File/Manga/FANTASY  
ROOT_MANGAH=G:/SecretManga
ROOT_TEST=E:/Download

# 🎬 Video paths (V_ prefix)  
V_MOVIE=E:/File/Video/MOVIE
V_ANIME=E:/DATA/Anime
V_JAVA=E:/DATA/JavaContent
V_ANIMEH=E:/DATA/AdultAnime

# 🎵 Music paths (M_ prefix)
M_MUSIC=E:/File/Music

# 🌐 CORS CONFIGURATION
# Cross-Origin Resource Sharing - Cho phép requests từ domains khác

# 💻 Development Origins (localhost variants cho development)
# Bao gồm: Express backend (3000), React dev server (3001), testing ports, local HTTPS
# QUAN TRỌNG: Phải bao gồm localhost:3000 và localhost:3001 để development hoạt động
CORS_DEV_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:3001,https://127.0.0.1:3001,http://localhost:3002

# 🔗 Remote Access Origins (Tailscale, ngrok, production domains)  
# Bao gồm: Tailscale network, tunnel services, production URLs
CORS_TAILSCALE_ORIGINS=http://your-device.tailscale.net:3001,http://your-device.tailscale.net:3000,https://your-domain.com

# 📝 Examples:
# CORS_DEV_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:3001,https://127.0.0.1:3001
# CORS_TAILSCALE_ORIGINS=http://mypc.tail12345.ts.net:3001,https://myapp.ngrok.io

# ⚠️  QUAN TRỌNG: 
# - CORS_DEV_ORIGINS phải có localhost:3000 (Express server) và localhost:3001 (React dev server)
# - Nếu thiếu sẽ bị CORS error khi development
# - Production chỉ cần CORS_TAILSCALE_ORIGINS cho remote access

# 🔐 SECURITY & ACCESS CONTROL
# Danh sách IP được phép truy cập
ALLOWED_IPS=127.0.0.1,192.168.1.10,192.168.1.99
# Danh sách hostname được phép (từ network info)
ALLOWED_HOSTNAMES=your-device-name,laptop-name

# � CONTENT SECURITY
# Danh sách root keys cần mật khẩu để truy cập (để trống = tất cả public)
SECURITY=ROOT_MANGAH,V_JAVA,V_ANIMEH
# Mật khẩu cho các keys được bảo mật
SECURITY_PASSWORD=your_secure_password_here


# --- IGNORE ---
# CẦN CẢ 2 vì:

# ALLOWED_HOSTNAMES = WHO can connect
# CORS_TAILSCALE_ORIGINS = WHERE browser can call FROM
# Đây là 2 layers bảo mật khác nhau, cần thiết cho security! 🛡️
# có thể Tự động generate CORS từ ALLOWED_HOSTNAMES nhưng chưa cần thiết