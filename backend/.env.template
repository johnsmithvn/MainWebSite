# 📁 backend/.env.template  
# 🔧 Environment Configuration Template
# 📝 Copy this file to .env và điền các giá trị thực tế

# 🌐 SERVER CONFIGURATION
PORT=3000
NODE_ENV=development    # không cần sửa do trong package.json đã set env thành production/development
ENABLE_HTTPS=true       # Bật HTTPS cho server (nếu có SSL cert)


# 📂 MEDIA ROOT PATHS
# Đường dẫn tới các thư mục media trên disk
 # đặt tên tùy ý nhưng nên có prefix để dễ quản lý
# 📚 Manga paths (ROOT_ prefix)
ROOT_MANGA=E:/File/Manga/DOW

# 🎬 Video paths (V_ prefix)  
V_MOVIE=E:/File/Video/MOVIE

# 🎵 Music paths (M_ prefix)
M_MUSIC=E:/File/Music

# 🌐 CORS CONFIGURATION
# Cross-Origin Resource Sharing - Cho phép requests từ domains khác
# Tự động generate URLs từ hostnames và ports - ít lặp lại hơn

# 💻 Development Origins (compact format)
# Format: hostname:port1,port2|protocols;hostname2:ports|protocols
CORS_DEV_CONFIG=localhost:3000,3001|http;127.0.0.1:3000,3001|http

# 🔗 Tailscale Network Access (auto-generation)
# domains từ Tailscale device và tailnet info
TAILSCALE_DEVICE=your-device-name
# Tailscale sau devices và trước ts.net
TAILSCALE_TAILNET=tail12345
# ports và protocols (http,https) để generate URLs
TAILSCALE_PORTS=3000,3001
TAILSCALE_PROTOCOLS=http,https


# ⚠️  QUAN TRỌNG: 
# - Development cần localhost:3000 (Express server) và localhost:3001 (React dev server)
# - Nếu thiếu sẽ bị CORS error khi development
# - Production chỉ cần Tailscale config cho remote access

# 🔐 SECURITY & ACCESS CONTROL
# Danh sách IP được phép truy cập (dùng để remote trong network)
ALLOWED_IPS=127.0.0.1,192.168.1.10,192.168.1.99
# Danh sách hostname được phép (từ network info)
ALLOWED_HOSTNAMES=your-device-name,laptop-name

# Danh sách root keys cần mật khẩu để truy cập (để trống = tất cả public)
SECURITY=V_MOVIE,ROOT_MANGA
# Mật khẩu cho các keys được bảo mật
SECURITY_PASSWORD=your_secure_password_here


# --- IGNORE ---
# CẦN CẢ 2 vì:

# ALLOWED_HOSTNAMES = WHO can connect
# CORS_TAILSCALE_ORIGINS = WHERE browser can call FROM
# Đây là 2 layers bảo mật khác nhau, cần thiết cho security! 🛡️
# có thể Tự động generate CORS từ ALLOWED_HOSTNAMES nhưng chưa cần thiết